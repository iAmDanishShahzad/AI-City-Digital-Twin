export const districtConstraints = Object.freeze({
  minimumNodeCount: 8,
  maximumNodeCount: 12,
  minimumDirectedEdgeCount: 12,
  maximumDirectedEdgeCount: 20,
  minimumVehicleCount: 8,
  maximumVehicleCount: 12,
  minimumSpawnTick: 0,
  maximumSpawnTick: 29,
  baseVehicleSpeed: 6,
  requiredClosableEdgeCount: 1,
});
